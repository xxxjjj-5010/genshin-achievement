# 原神成就统计

> 数据版本：游戏 7.0「无神怜爱的雪国」（上游 dvaJi/genshin-data release 0.62.0，2026-08-11）
> 1845 项成就 / 73 个合辑 / 14030 原石 · 纯静态站，零后端
> 支持 UIAF 一键导入 / 导出，不用再一条条手动勾

一个按 UID 分别统计原神成就进度的查漏补缺工具。数据每天自动跟上游同步，新增成就过一两天自己就长进来。

---

## 一、2026-09-18 升级：修掉一个会毁数据的隐患

原 `data.js` 停在 6.8（1759 项），且成就 key 是「文件::合辑::序号」。序号会随版本插入新成就整体后移 ——
实测直接用新数据覆盖，1759 条已保存进度里 **370 条会静默指向错误的成就**、518 条变无主数据。
（例：旧数据第 578 位的「无坚不摧」会被新的第 578 项顶替，你根本没做过却打着勾。）

**处理**：key 换成游戏内**稳定成就 id**（如 `81032`，1845 条全局唯一、不随版本变化），
并在 `data.js` 内置 `LEGACY_KEY_MAP` 迁移表，首次打开自动把旧进度转成新键、回写 localStorage，之后不再重复转换。

## 二、2026-09-21 升级（第一轮）：查漏补缺增强包

| 新功能 | 说明 |
| --- | --- |
| **合辑进度总览** | 筛选条最右侧「📊 合辑总览」。三个排序：差最少完成 / 剩原石最多 / 完成率最低；已完成的自动沉底；点任意一行直接跳到那个合辑 |
| **今日推荐** | 侧栏每天推 5 个未完成的成就，从「原石最多的前 30 个」里按「日期 + UID」确定性洗牌 —— 同一天内稳定不变，跨天自动换一批，两个 UID 各推各的 |
| **一键搜攻略** | 每条成就行尾悬停出现「攻略」按钮，点开新标签搜 Bing |
| **「7.0 新增」徽章** | 86 个 7.0 新成就带金色徽章，合辑标题上也显示「7.0新增·N项」；筛选条多一个「只看新增」 |
| **定位闪烁** | 从总览/今日推荐跳过去时，目标成就高亮闪两下，不用在长列表里找 |

## 三、2026-09-21 升级（第二轮）：一键导入进度

之前最大的痛点：**游戏里完成了什么，得自己一条条对照着回来勾**，1700 多条不可能手工维护。

### 怎么解决：用社区通用的 UIAF 标准

**UIAF**（统一可交换成就格式，v1.1）是 UIGF 组织制定的成就数据交换标准（<https://uigf.org>），
椰羊、Paimon.moe、胡桃工具箱、寻空等工具都认这个格式。它的 `id` 就是**游戏内原生成就 ID** ——
而本站的 key 早在 9-18 那次重构时就已经换成了同一套编号，所以两边能直接一一对应，不需要任何映射表。

### 三步搞定

1. 用 **YaeAchievement**（开源免费的成就导出工具，Windows）导出一次成就：
   关掉原神 → 双击运行它 → 它会自动帮你启动游戏 → 正常登录进游戏 → 它自动读取全部成就、读完游戏自动退出
   → 在它给出的列表里选「UIAF JSON File」，得到一个 `.json`
2. 打开本站，点顶栏「**导入进度**」
3. 选中那个 json —— 会先给你看一份报告（记录多少条、已完成多少、将新增多少），确认无误再点「确认导入」

导入是**合并**语义：只补上你已完成、网站还没勾的，**不会取消**你已有的进度。

> ⚠️ **两个坑**
> - YaeAchievement 这类工具需要读取游戏进程内存，属于第三方软件，严格讲会碰到米哈游用户协议的边界。
>   只读、不改游戏文件，社区长期使用风险很低，但**不是零风险**，请自行判断。不想用的话，下面第二节的批量功能也能省很多事。
> - 工具的 exe **不要和原神主程序放在同一个文件夹**，否则游戏会报「数据异常(31-4302)」。放桌面新建的文件夹里就行。
> - 它抓到的数据会缓存 1 小时。**多账号**可以在缓存有效期内依次登录、分别导出，再回本站切到对应 UID 分别导入。

### 顺带新增的功能

| 新功能 | 说明 |
| --- | --- |
| **顶栏「导入进度」** | 同时吃两种文件：UIAF 成就文件、本站「导出备份」的 JSON（自动识别）。导入前有预览报告，导入后可撤销 |
| **批量勾选** | 每个合辑卡片右上角有「**整组完成**」按钮 —— 某个合辑在游戏里已经全清了，一下点完，不用逐条勾。全完成时按钮自动变「整组清空」 |
| **搜索结果批量完成** | 搜索面板右上角「全部标记完成」，把当前搜索命中的成就一次勾完 |
| **一键撤销** | 上面这些批量操作和导入，执行后底部会弹提示条，12 秒内可以点「撤销」原样还原 |
| **导出 UIAF** | 导出弹窗里新增。你的进度可以随时带去椰羊 / Paimon.moe / 胡桃工具箱等任何支持 UIAF 的工具 |
| **完成时间记录** | 导入时会保留每条成就的完成时间；手动勾选也记时间。为以后的「最近完成」等功能留好了数据 |

### 一个意外的好处：版本哨兵

如果导入时出现「有 N 条成就本站数据里还没有」，说明**游戏版本比本站数据新** ——
这等于提前告诉你新版本已经上线、且有新成就了。等每天上午 10 点自动同步跑完，再导入一次就能补上。
（这些多出来的 id 会被明确列出来，**不会被静默丢弃**。）

## 四、包含文件

| 文件 | 说明 |
| --- | --- |
| `index.html` | **从 9-18 至今一直未改动**（所有新功能都由 JS 动态注入，不需要改模板） |
| `app.js` | 全部交互逻辑（含 UIAF 导入导出、批量勾选、撤销） |
| `style.css` | 样式（含 9-18 修复的一处 CSS 语法 bug + 本轮新组件样式） |
| `data.js` | 当前版本数据，key 用稳定 id + 版本标记 `v` |
| `tools/build-data.mjs` | 数据生成脚本：从上游拉取并重建 `data.js` |
| `.github/workflows/sync-achievements.yml` | 每天 UTC 02:00（北京时间 10:00）自动跑生成器，数据有变化才提交 |

## 五、为什么已保存的进度不会丢

- 成就 key 用游戏内稳定 id，上游怎么增删改名都不影响已有勾选。
- `data.js` 带 1759 条旧键 → 新 id 的完整迁移表；首次进入某个 UID 时自动转换并回写。
- 迁移保真度已**全量实测**：1759 条旧键全部落到存在的成就上，其中 1758 条名称完全一致，
  唯一不同的「QUEST FAILED」是上游自己把它改成了「QUEST FAILED  OR …」（描述也从「跟丢了悬赏目标…」扩充为整句），
  新数据里没有任何名字叫 `QUEST FAILED` 的条目，确认映射正确。
- 另建议养成习惯：**每次版本更新后用「导出备份（JSON）」或「导出 UIAF」存一份**。
  备份可原样导入（导入是合并，不覆盖）；UIAF 还能带着进度迁移到别的成就工具。

## 六、需要长期维护的只有一处

`app.js` 顶部的 `BLOCKED` 名单 —— 当前版本暂时拿不到的成就。它们会被标黄并从完成度分母里排除。
现在有 3 条：`苍明如冰`（至冬七天神像满级）、`「影域老司机」`（影域解析等级满级）、`伟业的遂成`（同时持有 22 种月谕圣牌）。
每次版本更新后复核一次（游戏里点一下就知道能不能拿）。**所以主进度显示的分母是 1842，不是 1845。**

版本中文名不用手动维护了：`tools/build-data.mjs` 里的 `VERSION_NAMES` 已收录 7.0 / 7.1，
新版本想显示中文名在那里补一行即可，没补就沿用上一版名称。

## 七、数据是怎么自动跟版的

```
dvaJi/genshin-data（上游，版本更新后 1 天内同步）
        ↓  每天北京时间 10:00 / 也可在 Actions 页手动触发
tools/build-data.mjs
        ↓  ① 拉取 73 个合辑 JSON
        ↓  ② 重建 data.js（key 用稳定 id）
        ↓  ③ 标记新成就的版本号（对比 6.8 基线 + 上一版 data.js 的 v 字段）
        ↓  ④ 内容没变就退出，不产生提交
GitHub Actions 提交 data.js → GitHub Pages 自动部署
```

生成器的三个细节：

- **版本标记自给自足**：不用额外的基线文件。基线 = `LEGACY_KEY_MAP` 的 1759 个 id，
  上一版的 `v` 字段直接从 `data.js` 里读回来，既不在基线、上一版也没记录的 id 才算本次新增。
  实测模拟 7.1 到来时，新增的只会被标成 7.1，已有的 86 条 7.0 标记不会被打乱。
- **多源降级**：文件内容按 `raw.githubusercontent.com` → `cdn.jsdelivr.net` → GitHub API 依次尝试。
  某些网络环境（含本机代理）会拦 `raw` 域名，多一路备用可以少一次「同步失败」。首次成功的源会被记住，后续直接用。
- **8 路并发下载**：73 个合辑并行拉取，实测本机 57 秒跑完（网络好时更快）。

## 八、本地验证结果（2026-09-21）

第一轮（静态与结构检查）：

```
=== 1. data.js 结构 ===      ✅ 1845 项   ✅ 73 合辑   ✅ 14030 原石   ✅ key 全局唯一
                             ✅ 86 条 7.0 版本标记   ✅ META 与实测值一致
=== 2. 旧进度迁移 ===        ✅ 1759 条旧键全部命中   ✅ 1758 条名称一致
                             ✅ 剩余 1 条为上游已知改名，已核对
=== 3. DOM 依赖 ===          ✅ app.js 引用的 id 全部存在
=== 4. 样式覆盖 ===          ✅ 模板用到的 class 全部有样式定义
=== 5. BLOCKED 名单 ===      ✅ 3 条 id 均在数据中 → 可完成分母 1842
=== 6. 生成器 ===            ✅ 实跑 status=0，两次生成 sha256 完全一致（真幂等）
```

第二轮（jsdom 真实跑页面，三个独立实例，**失败 0 项 / 运行时错误 0 条**）：

```
【老功能回归】
  ✅ 73 合辑 / 1845 条渲染    ✅ 分母 1842（BLOCKED 正确排除）
  ✅ 86 个「7.0新增」徽章      ✅ 1845 个攻略按钮
  ✅ 今日推荐 5 条            ✅ 合辑总览 73 行
  ✅ 「只看新增」筛出 86 条     ✅ 切回「全部」恢复 1845

【批量勾选与撤销】
  ✅ 合辑「天地万象」整组完成 → 985 条一次性落盘
  ✅ 按钮自动变为「整组清空」，再点即清空
  ✅ 撤销后存档与统计完全还原
  ✅ 搜索结果「全部标记完成」4 条落盘，面板勾选状态同步

【UIAF 导入】
  ✅ 121 条记录正确识别：已完成 82 / 未完成 36 / 将新增 82
  ✅ status=3「已领奖」也算完成（UIAF spec）
  ✅ 3 个未知 id 被明确列出，未污染存档
  ✅ 完成时间按秒→毫秒落盘；9999-12-31 占位值被正确识别
  ✅ 二次导入「将新增 0」，合并语义正确、不重复计数
  ✅ 坏文件报错明确，不静默失败

【UIAF 导出】
  ✅ uiaf_version = v1.1
  ✅ 1845 条记录、字段全部为数字类型（spec 硬要求）
  ✅ 已完成条数与存档一致；未完成项 timestamp 一律为 0

【兼容】
  ✅ 本站备份 JSON 仍可正常导入并正确识别格式
```

## 九、要注意的架构边界

- **是纯静态站**：服务器上只有 4 个固定文件，没有后端和数据库。所有「动」的部分（渲染、搜索、筛选、算完成度）都在你的浏览器里跑。
- **进度只存在这台设备的这个浏览器里**：按 UID 分键存在 localStorage。换电脑、换浏览器、清缓存 = 进度归零，
  服务器端根本不知道你是谁、做到哪了。所以「导出备份（JSON）」/「导出 UIAF」是你唯一的云端，建议每个版本更新后存一次。
  （UIAF 导入导出全程在你的浏览器里完成，**文件不会上传到任何服务器**。）
- 每天自动更新只是把 `data.js` 这个静态文件换成新版再重新部署 —— 相当于每天出一版新报纸，不是开了个直播间。
